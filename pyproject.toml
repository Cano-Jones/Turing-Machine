[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "Turing-Machine"
version = "2.0.0"
description = "Turing-Machine implementation in python"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}
